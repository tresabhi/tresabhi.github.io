---
import type { FrontMatter } from "../core/globContents";
import ResponsiveImage from "./ResponsiveImage.astro";

type Props = FrontMatter;

const { hero } = Astro.props;
---

{hero && <ResponsiveImage src={hero} alt="Hero image" />}
